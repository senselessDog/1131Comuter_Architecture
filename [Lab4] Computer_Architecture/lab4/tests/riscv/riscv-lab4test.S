//=========================================================================
// riscv-lab4test.S
//=========================================================================

#include "riscv-macros.h"

        TEST_RISCV_BEGIN

        
        //-----------------------------------------------------------------
        // out-of-order commit tests
        //----------------------------------------------------------------- 
        // 參數說明：
        // fast_val = 10 (加法的值)
        // slow_val1 = 3, slow_val2 = 2 (乘法的兩個運算元)
        // result = 10 (期望的in-order結果)
        TEST_WAW_HAZARD(10, 3, 2, 10)
        TEST_WAW_HAZARD(15, 5, 10, 15)
        //-----------------------------------------------------------------
        // Bypassing from ROB tests
        //-----------------------------------------------------------------
        TEST_BYP_FROM_ROB( 10, 3, 2, 10 ,1)
        TEST_BYP_FROM_ROB( 18, 6, 8, 18 ,1)


        TEST_RISCV_END